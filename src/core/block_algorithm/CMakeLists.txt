add_library(BlockAlgorithmLib INTERFACE)
target_include_directories(BlockAlgorithmLib INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(BlockAlgorithmLib INTERFACE cxx_precompile_header math)
